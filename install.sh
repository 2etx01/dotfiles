#!/usr/bin/env bash
ln -fs `pwd`/vimrc    "${HOME}/.vimrc"
ln -fs `pwd`/tmux.conf    "${HOME}/.tmux.conf"
ln -fs `pwd`/gdbinit    "${HOME}/.gdbinit"
ln -fs `pwd`/gitconfig    "${HOME}/.gitconfig"
ln -fs `pwd`/zshrc    "${HOME}/.zshrc"
ln -fs `pwd`/vim "${HOME}/.vim"
ln -fs `pwd`/bashrc    "${HOME}/.bash" 
